name: Fix project highlights
on:
  push:
    paths:
      - '.github/workflows/fix-project-highlights.yml'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Add remaining highlights
        run: python .github/scripts/fix-project-highlights.py
      - name: Commit website update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git rm .github/scripts/fix-project-highlights.py .github/workflows/fix-project-highlights.yml
          git commit -m "Complete key career project highlights"
          git push
