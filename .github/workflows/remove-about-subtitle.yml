name: Remove About subtitle
on:
  push:
    paths:
      - '.github/workflows/remove-about-subtitle.yml'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Remove About subtitle
        run: python .github/scripts/remove-about-subtitle.py
      - name: Commit website update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git rm .github/scripts/remove-about-subtitle.py .github/workflows/remove-about-subtitle.yml
          git commit -m "Remove About section subtitle"
          git push
